# content/activities

5 hand-seeded activities for repository cold-start and demo insurance (used as fallback if live generation is slow/unavailable during the demo).

Status: placeholder-only — no seed activities committed yet. Without these, the demo's manual-fallback path (D6) has nothing to serve if live generation is slow; this is a pre-event to-do, not done.
